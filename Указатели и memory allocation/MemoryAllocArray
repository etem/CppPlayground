#include <iostream> 

int main()
{
    int *p = new int[5]; //Allocate memory on the heap for a new array.
    
    std::cout << *p; 
    
    std::cout << std::endl;
    
    delete []p; // Free up the memory allocated for p.
    
    std::cout << *p;

}